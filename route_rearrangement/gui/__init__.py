"""A route viewer borrowed from ``synthesis_extraction.gui``: render each enumerated
ordering of one literature route as a left-to-right reaction scheme, and browse them
next to the original so the rearrangements can be eyeballed for chemical logic."""
